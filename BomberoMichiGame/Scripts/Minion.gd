extends "res://personajes/minions/minions.gd"

# Wrapper compatibility script.
# Algunas escenas (exportadas anteriormente) referencian `res://Scripts/Minion.gd`.
# Este archivo simplemente extiende el script real en `personajes/minions/minions.gd`.

# Si necesitas agregar compatibilidad adicional (exports, señales), añadelos aquí.
